server:
  port: 8080

spring:
  application:
    name: llm-assistant-backend
  servlet:
    multipart:
      enabled: true
      max-file-size: 50MB # 🟢 单个文件最大限制 (比如改成 50MB)
      max-request-size: 100MB # 🟢 一次请求所有文件的总大小限制 (比如改成 100MB)

  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/llm_assistant?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${SPRING_DATASOURCE_USERNAME:root}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver

  ai:
    zhipuai:
      api-key: ${SPRING_AI_ZHIPUAI_API_KEY:} # ???????????? API Key
      chat:
        options:
          model: glm-4-flash
          temperature: 0.7
        memory:
          repository:
            jdbc:
              # 告诉它总是初始化表结构（如果表不存在会自动创建）
              initialize-schema: always

  logging:
    level:
      org.springframework.ai: DEBUG

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true # ??????? user_name ?? Java ? userName
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # ?????? SQL?????
