# 本文件为本地示例配置：复制为 application-local.yml（会被 .gitignore 忽略）
# 然后用 profile 启动：mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/llm_assistant?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: your_db_password

  ai:
    zhipuai:
      api-key: your_zhipuai_api_key
